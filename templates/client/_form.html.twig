{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn mt-4  text-white p-3 rounded-md w-full bg-sky-600 hover:bg-sky-700 focus:ring-2 focus:ring-orange-500">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
