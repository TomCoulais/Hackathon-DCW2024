{% extends 'base.html.twig' %}

{% block title %}Hackaton | Nouveau client{% endblock %}

{% block body %}
<div class="flex items-center justify-center min-h-screen ">
    <div class="max-w-2xl w-full p-8 bg-sky-950 rounded-lg">
        <div class="w-full text-left mb-6">
            <a href="{{ path('app_clients') }}" class=" text-sky-700 hover:text-sky-400 font-semibold text-white flex items-center">
               &larr; Retour à la liste des clients
            </a>
        </div>
        <h1 class="text-3xl font-bold text-center text-white mb-8">Création d'un nouveau client</h1>

        <div class=" p-8 rounded-lg">
            {{ include('client/_form.html.twig') }}
        </div>
    </div>
</div>

{% endblock %}
