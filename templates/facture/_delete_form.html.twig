<form method="post" action="{{ path('app_facture_delete', {'numero': facture.numero}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.numero) }}">
    <button class="btn">Delete</button>
</form>
